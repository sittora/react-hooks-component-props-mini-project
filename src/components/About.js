import React from "react";
// import imageLogo from './path/to/logo.jpg';

function About({about}) {
    return (
        <aside>
         <img src ="https://via.placeholder.com/215g" alt={`blog logo`}/>
         <p>{about}</p>
        </aside>
    )}
  
  export default About;